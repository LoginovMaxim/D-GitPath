using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrafficGeneration : MonoBehaviour
{
    public GameObject car;

    void Start()
    {
        //Rgen = new RoadGenerator();
        //Debug.Log("Rgen.graphNodes : " + RoadGenerator.graphNodes[Random.Range(0, RoadGenerator.graphNodes.Count)]);
        //PassengerCar passengerCar = new PassengerCar();

    }
}
